// TODO: Implement user queries
// This file will mirror Debatium-backend/db/users/queries.py
//
// Queries to implement:
// - getUserByEmail(pool, email)
// - getUserProfileData(pool, userId)
// - getPublicProfileData(pool, username)
// - searchUsersData(pool, term)
// - insertUser(pool, user)
// - updateUser(pool, userId, data)
// - getUserPasswordHash(pool, userId)
// - insertUserAvailability(pool, availability)
// - getUserCalendarData(pool, userId)
// - getUserAvailability(pool, availabilityId, userId)
// - updateUserAvailability(pool, availability)
// - deleteUserAvailability(pool, availabilityId, userId)
